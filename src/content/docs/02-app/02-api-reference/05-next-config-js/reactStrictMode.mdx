---
title: reactStrictMode
description: 完整的Next.js运行时现在符合严格模式，学习如何选择加入
---

{/* The content of this doc is shared between the app and pages router. You can use the `<PagesOnly>Content</PagesOnly>` component to add content that is specific to the Pages Router. Any shared content should not be wrapped in a component. */}

> **很高兴知道**: 由于 Next.js 13.4，`app`路由器默认情况下的`Strict Mode`为`true`，因此上述配置仅适用于`pages`。您仍然可以通过设置`reactStrictMode:false`来禁用严格模式。

> **建议**: 我们强烈建议您在 Next.js 应用程序中启用严格模式，以便更好地为 React 的未来做好准备。

React 的[严格模式](https://react.dev/reference/react/StrictMode)是一个仅用于突出应用程序中潜在问题的开发模式功能。它有助于识别不安全的生命周期、遗留 API 使用情况以及许多其他功能。

Next.js 运行时与严格模式兼容。要选择进入严格模式，请在`next.config.js`中配置以下选项：

```js filename="next.config.js"
module.exports = {
  reactStrictMode: true,
};
```

如果您或您的团队还没有准备好在整个应用程序中使用严格模式，那没关系！您可以使用`<React.StrictMode>`。
